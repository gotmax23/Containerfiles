---
# Generated by src/systemd/generate.py
name: "Debian.forky - systemd"

'on':
  push:
    branches:
      - main
    paths:
      - "Containerfiles/systemd/Debian/Debian.forky.Containerfile"
      - ".github/workflows/systemd-Debian.forky-ci.yml"
      - ".github/workflows/reusable-container-build.yml"
  schedule:
    - cron: "10 1 * * 0"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build_push:
    name: Build and Push container images to quay.io
    permissions:
      contents: read
    uses: ./.github/workflows/reusable-container-build.yml
    with:
      context: "./Containerfiles/systemd/Debian"
      containerfile: "./Containerfiles/systemd/Debian/Debian.forky.Containerfile"
      platforms: "linux/amd64,linux/arm64"
      image_title: "Debian image with systemd"
      image_description: "Debian image with systemd for Ansible testing using Podman and Molecule"
      project_path: "Containerfiles/systemd"
      documentation_path: "Containerfiles/systemd/README.md"
      tags: |
        quay.io/gotmax23/debian-systemd:forky
        quay.io/gotmax23/debian-systemd:testing
        quay.io/gotmax23/debian-systemd:14
    secrets:
      registry_username: ${{ secrets.QUAY_USERNAME }}
      registry_password: ${{ secrets.QUAY_PASSWORD }}
