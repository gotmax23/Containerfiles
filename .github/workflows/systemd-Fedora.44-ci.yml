---
# Generated by src/systemd/generate.py
name: "Fedora.44 - systemd"

'on':
  pull_request:
    paths:
      - "Containerfiles/systemd/Fedora/Fedora.44.Containerfile"
      - ".github/workflows/systemd-Fedora.44-ci.yml"
      - ".github/workflows/reusable-container-build.yml"
  push:
    branches:
      - main
    paths:
      - "Containerfiles/systemd/Fedora/Fedora.44.Containerfile"
      - ".github/workflows/systemd-Fedora.44-ci.yml"
      - ".github/workflows/reusable-container-build.yml"
  schedule:
    - cron: "10 1 * * 0"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build_push:
    name: Build and Push container images to quay.io
    permissions:
      contents: read
    uses: ./.github/workflows/reusable-container-build.yml
    with:
      context: "./Containerfiles/systemd/Fedora"
      containerfile: "./Containerfiles/systemd/Fedora/Fedora.44.Containerfile"
      platforms: "linux/amd64,linux/arm64"
      image_title: "Fedora image with systemd"
      image_description: "Fedora image with systemd for Ansible testing using Podman and Molecule"
      project_path: "Containerfiles/systemd"
      documentation_path: "Containerfiles/systemd/README.md"
      tags: |
        quay.io/gotmax23/fedora-systemd:44
        quay.io/gotmax23/fedora-systemd:latest
    secrets:
      registry_username: ${{ secrets.QUAY_USERNAME }}
      registry_password: ${{ secrets.QUAY_PASSWORD }}
