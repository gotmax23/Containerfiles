---
# Generated by src/systemd/generate.py
name: "SLES.16.0 - systemd"

'on':
  push:
    branches:
      - main
    paths:
      - "Containerfiles/systemd/SLES/SLES.16.0.Containerfile"
      - ".github/workflows/systemd-SLES.16.0-ci.yml"
      - ".github/workflows/reusable-container-build.yml"
  schedule:
    - cron: "10 1 * * 0"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build_push:
    name: Build and Push container images to quay.io
    permissions:
      contents: read
    uses: ./.github/workflows/reusable-container-build.yml
    with:
      context: "./Containerfiles/systemd/SLES"
      containerfile: "./Containerfiles/systemd/SLES/SLES.16.0.Containerfile"
      platforms: "linux/amd64,linux/arm64"
      image_title: "SLES image with systemd"
      image_description: "SLES image with systemd for Ansible testing using Podman and Molecule"
      project_path: "Containerfiles/systemd"
      documentation_path: "Containerfiles/systemd/README.md"
      tags: |
        quay.io/gotmax23/sles-systemd:16.0
        quay.io/gotmax23/sles-systemd:latest
        quay.io/gotmax23/sles-systemd:16
    secrets:
      registry_username: ${{ secrets.QUAY_USERNAME }}
      registry_password: ${{ secrets.QUAY_PASSWORD }}
