---
# Generated by src/systemd/generate.py
name: "opensuse.16 - systemd"

'on':
  pull_request:
    paths:
      - "Containerfiles/systemd/opensuse/opensuse.16.Containerfile"
      - ".github/workflows/systemd-opensuse.16-ci.yml"
      - ".github/workflows/reusable-container-build.yml"
  push:
    branches:
      - main
    paths:
      - "Containerfiles/systemd/opensuse/opensuse.16.Containerfile"
      - ".github/workflows/systemd-opensuse.16-ci.yml"
      - ".github/workflows/reusable-container-build.yml"
  schedule:
    - cron: "10 1 * * 0"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build_push:
    name: Build and Push container images to quay.io
    permissions:
      contents: read
    uses: ./.github/workflows/reusable-container-build.yml
    with:
      context: "./Containerfiles/systemd/opensuse"
      containerfile: "./Containerfiles/systemd/opensuse/opensuse.16.Containerfile"
      platforms: "linux/amd64,linux/arm64"
      image_title: "opensuse image with systemd"
      image_description: "opensuse image with systemd for Ansible testing using Podman and Molecule"
      project_path: "Containerfiles/systemd"
      documentation_path: "Containerfiles/systemd/README.md"
      tags: |
        quay.io/gotmax23/opensuse-systemd:16
        quay.io/gotmax23/opensuse-systemd:latest
        quay.io/gotmax23/opensuse-leap-systemd:16
        quay.io/gotmax23/opensuse-leap-systemd:latest
    secrets:
      registry_username: ${{ secrets.QUAY_USERNAME }}
      registry_password: ${{ secrets.QUAY_PASSWORD }}
